1. Instance variable is also knows as a class variable, you have to create an object of the class inorder to access those. On the other hand a static variable doesnt need an object of the class, you can direcly access the variable by classname.variable.
2. Inside objects of class we can observe encapsulation feature because the method used in the class are encapsulated inside the objects.
3. Instance variables needs an object of the class, if accessed directly it will show an error. 